#!/bin/sh
set -eu
for gh_dir in /opt/homebrew/bin /usr/local/bin "$HOME/.homebrew/bin"; do
  if [ -x "$gh_dir/gh" ]; then PATH="$gh_dir:$PATH"; fi
done
export PATH
exec "$HOME/.hermes/hermes-agent/.venv/bin/python" "$HOME/.agents/skills/omarchy-cloud/scripts/pr_evidence.py" "$@"
