> Engineering reference from the initial implementation. For current setup, use
> [Agent setup](AGENT-SETUP.md) and [Host installation](HOST-INSTALL.md). Historical
> scripts and machine/image receipts are not fresh-install instructions or proof
> that a new deployment has passed these checks.

# Release A gap audit

Historical gap snapshot from the successful auth rollout on 2026-09-17; see BUILD-STATUS.md for newer bound Hermes execution evidence. Implementation has resumed. As of 2026-09-18, the original operator requires Grok 4.6/xhigh through call-grok for checkpoint reviews, replacing the older Fable requirement. Scope is [SPEC.md](SPEC.md) §§14–17: P00–P15 and P22 core, Hermes runtime with pstack and two actual model roles required (H01–H08 in SPEC-FIRST-CONTRACT.md). See [architecture correction](HERMES-RUNTIME-CORRECTION.md). This is an acceptance-gap audit, not a request to expand Release A. Source and evidence are changing concurrently; a newer bound receipt can close a row without repeating unrelated passing checks.

**Release A production cutover is not yet qualified.** Real Claude execution is now proven: provider smoke (private historical record, not distributed), live record/source/image binding (private historical record, not distributed) show a correct date-validation change, protected verification, live public events, immutable file downloads and a successful reconstructed follow-up on Omarchy. Do not repeat the older “provider disabled” deployment snapshot as current status. CP3 HTTP smoke (private historical record, not distributed) also proves binary supplied-input roundtrip. Bound lifecycle evidence (private historical record, not distributed) covers real Docker cleanup/failure/restart cases; runtime qualification (private historical record, not distributed) proves hard disk ENOSPC, confinement and allowed GitHub HTTPS. Those successes do not cover every gate below.

## Required gaps

**Priority runtime gap:** the current execution is direct Claude Code, not Hermes. Hermes headless launch, isolated backend auth and actual Hermes task qualification are mandatory for Release A. Existing provider receipts remain valid for their narrower direct-Claude scope.

| Gate | Concrete remaining capability or evidence | Closure |
|---|---|---|
| **P22/P03/T16: backup and restore** | Canonical quiesced Omarchy → encrypted SSH stream → isolated Mac restore passed: independent receipt (private historical record, not distributed), backup (private historical record, not distributed), restore (private historical record, not distributed). SQLite integrity, 36 artifact hashes, 5 input hashes, 17 closed attempts and zero active restored clients verified. Earlier `p22-live-backup-create` overlapped a qualifier and is superseded. | Selected logical-state recovery is proven. Replacement-host service activation, boot/power recovery, provider reauthentication/native continuation and production cutover remain unqualified. See [scope and canonical receipt chain](OFFLINE-BACKUP-RESTORE.md); no schedule or deletion was enabled. |
| **P22/T16: host operations** | Supervisor recovery, private SSE reconnect and a real synthetic volume unmount/remount are proven; remount receipt (private historical record, not distributed). V2 boot enablement is configured. [Operations runbook](HOST-OPERATIONS.md) distinguishes liveness, readiness and recovery. | Physical reboot, AC/lid/sleep and host network-loss rehearsal remain. Root is LUKS encrypted; arrange physical unlock before reboot. Remount proof is not a reboot claim. |
| **P11/P22: retention controls** | Rolling30-day deadline, explicit keep/deadline, CAS/idempotency and bounded dry-run manifest deployed. Reversible live controls and stale409 rejection passed in `auth-live-integration-receipt.json`. | Purge501, no scheduled executor/adopted deletion policy. Future execution needs fresh identity checks, shared-reference/native-secret handling and tombstones; no artifact purge occurred. |
| **P05/P10/P13/P15/T17: repository/checkpoint delivery** | Registered immutable local Git snapshot, protected baseline, generation-bound patch/files and reconstructed follow-up are deployed. Real provider qualification (private historical record, not distributed) passed protected checks, downloaded patch apply/hash equality, cross-owner denial and live SSE disconnect/replay with no omissions or duplicates. | Bounded repository path is proven. Fable checkpoint review/disposition pending. Multi-repository delivery, remote Git credentials, push and PR remain unsupported. |
| **P14: immutable environment lifecycle** | Version-addressed manifests, image/CLI readiness qualification, CAS activation, immutable attempt snapshots and protected verifier hashes deployed for three projects. Deployment receipt (private historical record, not distributed); [review disposition](P14-REVIEW-DISPOSITION.md). | Prebuilt-image qualification is proven; general image build service/install/startup commands and per-environment resource/image divergence are not implemented. Current profiles require deployment-matching resources and image. |
| **P02/P06/P08/T05/T09/T20: credential qualification** | Dedicated official Claude inference and native isolation are proven. Local corrected auth unit adds safe missing/invalid/structured rejection/rate-limit handling, persistent private quarantine, explicit replacement/restart and per-attempt token redaction. Fable findings independently addressed;51 unit tests,106 integration/readiness tests and14 actual networkless container assertions passed. | Corrected auth/image rollout is live and independently read back in `auth-deployment-parent-readback.json`. Dedicated state initialized, v2 versions activated, old image retained; three real attempts and six agent/verifier image bindings passed in `auth-live-integration-receipt.json`. No automatic refresh claim; old-image adapter limitations remain explicit. |
| **P07/P10: provider failures** | Structured authentication/429 handling, explicit unknown usage, durable quarantine and terminal staged-token cleanup deployed. Real CLI401 capture and classifier replay passed; three actual provider attempts verified and left zero staged tokens. | Old pinned408eb adapter lacks new structured failure classification; no automatic token refresh/replay. Bundle/API/UI and asynchronous resource integration remain separate. |
| **T18: supplied-input document task** | Real Claude supplied-input report and independent content rubric passed: provider receipt (private historical record, not distributed), rubric (private historical record, not distributed). Exported report reconciles supplied synthetic booking totals and cancellation handling. | Closed for bounded supplied-input task. Recorded service outcome remains unverified because no software verifier applies; independent content review is separate. Open-web research remains deferred. |
| **T03/T07–T11/T19–T21: residual failure matrix** | Existing bounded memory/PID/log/isolation/canary proofs remain. Completion commit crash gap fixed; real Docker process-exit probes before/after commit recovered verified with no owned containers. Fable findings independently corrected; deployed delta only private-directory mode acceptance. | Physical Docker rotation footprint and whole-host outage/reboot matrix remain. Do not equate observed resource samples or process recovery with those host-level gates. |
| **P12 and §17: compatibility/cutover** | Opt-in `cloud-compat` implements legacy verbs through v2.21 tests and HTTPS named-client qualification (private historical record, not distributed) passed with unchanged counters/legacyPID. Read-only legacy inventory (private historical record, not distributed) found five retained workspaces, no current legacy containers. [Migration plan](LEGACY-WORKSPACE-MIGRATION.md) preserves originals and excludes credentials. | Actual workspace-only import, explicit legacy-principal/token migration, existing-client endpoint cutover and rollback proof remain open. New CLI is not v1 wire compatibility. Do not restart v1: its startup removes old labeled containers. No old jobs deleted or claimed imported. |

P15 bundle/API/CLI and dashboard active-version defaults were deployed and independently checked: evidence/p15-api-live-binding.json and evidence/p15-live-browser.json. Dashboard input UI remains local; asynchronous resource integration has source-bound local and disposable Docker evidence, not a production rollout. The full-spec pause recorded here was subsequently lifted by the original operator.

## Not Release A blockers

- **B:** visual dashboard acceptance, browser automation/preview, terminal/browser takeover, approval executor and Git push/PR integrations (P16–P19; T12/T14/T15 and full visual T17).
- **C:** additional Codex/Grok/other backend qualification behind Hermes, selected MCP/skills and notifications (P20–P21). Hermes itself, pstack routing and at least two actual model roles are required for A. The full requested cross-provider role map remains required for C, not silently omitted when unavailable.
- **Later:** open-web research (T18R), second worker and VM/hostile multi-tenant isolation (P23). Native conversation resume may remain explicitly unsupported; reconstructed continuation already has a passing receipt.

Close implementation gaps and missing receipts before claiming the corresponding gate. Checkpoint reviews now use Grok 4.6/xhigh through call-grok; the latest CP2 written verdict was evidence-only REVISE, subsequently addressed by bound receipts, not a written blanket Release A PASS. Preserve these scope distinctions in the final handoff.
