> Engineering reference from the initial implementation. For current setup, use
> [Agent setup](AGENT-SETUP.md) and [Host installation](HOST-INSTALL.md). Historical
> scripts and machine/image receipts are not fresh-install instructions or proof
> that a new deployment has passed these checks.

# Control-plane checkpoint — 2026-09-17

Current parent task: build cloud workbench on Omarchy with Fable checkpoints. No new rollout executed by this delegate after the repository checkpoint.

- Frozen next source: `evidence/auth-retention-source-snapshot`, manifest SHA256 `32483857f295ae19b9309cd8d53ae2a7947122a5718d0fcd357cd11640666cc4`.
- Root-owned remote stage: `/tmp/cwb-auth-20260917T2040/snapshot`.
- Deployment proposal: `scripts/deploy-auth-checkpoint.py`, 7 focused tests pass. Default validates only. Parent must inspect Fable before `--execute`.
- Read-only live plan: `evidence/auth-retention-deploy-plan.json`; exact live config/base/source/DB/Docker quiescence checks passed. Legacy PID974255 unchanged.
- Exact provider/client secret scan passed: `evidence/auth-retention-exact-secret-scan.json`; no values exposed.
- Initial broad Fable review session69123 ended error_max_budget_usd with no verdict; preserve its receipt. Parent explicitly authorized one corrective retry: completed session13595, outputs `reviews/checkpoint-auth-image-retry-fable.md` / `reviews/checkpoint-auth-image-retry-receipt.txt`; exact56935-byte rawpack `evidence/auth-image-integration-retry-pack.md`; same8turn/$4/1200s. Retry returned valid REVISE, estimated2.42918425. Deployment findings fixed independently; runtime owns token cleanup/quarantine and CLI proof. No more review loop authorized.
- Parent source may advance in new standalone result_bundle.py; it is expressly NOT in this snapshot/deployment.
- Parent full app suite508 passed; local runner/environment/credential57 and deployment7 passed. New snapshot binds parent repository fixes and retention.
- Image selection is local: worker qualified_images allowlist, both agent/verifier cloned from trusted deployment policy with snapshot image; old sessions/recovery/followup retain old408eb image. No shared runtime mutation. Old image retains generic legacy auth-failure behavior.
- Repository Fable previously REVISE; confirmed LF/control patch corruption, large patch fatal path, mode omission, excluded baseline false deletion. Parent corrected these; see parent disposition and tests. Executed live old receipt remains historical evidence, not proof of corrections.
- Smoke continuation guard now enforces identical host/config/source/image/commit; 14 tests. Prior receipts match retrospectively. Executed driver preserved separately, current driver not rerun live. Binding upgraded with actual source/config/driver hashes, test XML digest and sanitized worker policy. Initial host checker failure cause is labeled inference; original stderr/exit not retained.

Deployment prerequisites: retain old image and immutable v1 manifests; build network-none from `sha256:b08595b4ff44def60a66aa0bbd5614cdee71f2852088ed27f774af7f7d94e1f6`, qualify v2 manifests for all3projects, initialize CredentialState only as cloud-worker, no legacy changes/reboot/secret values. Script copies registry and preserves old snapshots, backs root-private DB/config/source, checks owned job/proxy inventory before/after quiescence, restarts only v2 and authenticated readiness. Console entrypoint metadata not installed; Python -m cloudworkbench.compat supported. No retention purge/executor is added.

Prepared outside frozen source: `scripts/qualify-auth-image-integration.py`, not executed. Parent approved3 scoped real-provider attempts after reviewed rollout: newrepo-v2 task+followup and existing historicalrepo-v1 followup. Driver verifies API+worker hashes and fresh matched deployment receipt, exact images/snapshots, patch apply and resulting content+executable modes, SSE, and reversible retention keep/CAS/idempotency/dry-run controls. No live token rotation; synthetic separate auth-state proof belongs to runtime agent.

ROLLOUT HOLD (parent): T03 process death after verifier cleanup but before completed DB transition incorrectly recovers as failed. Parent owns runner.finish/monitor completion-intent fix and tests/test_completion_recovery.py. Existing frozen candidate MUST NOT deploy, even if current auth/image review passes. Let review finish scoped; then await parent correction diff/tests, rebind frozen runner+tests and re-scan staged source before rollout.

Deployment corrections complete: script now synthesizes configs and runs read-only credential/state permission probe as cloud-worker in validate mode; initializes only in execute before service stop/publication; verifies actual pinned gateway egress.py; blocks created orphans; receipts report started/observed active services and bounded HTTP readiness errors. Tests12 and environment17 (including runtime=None production verifier constructor) passed. Actual corrected preflight on Omarchy passed, with initialized=false and both services active; evidence/auth-deployment-corrected-preflight.json, binding auth-deployment-corrections-binding.json, disposition reviews/auth-image-deployment-disposition.md. Parent completion recovery live before/after-commit qualification passed; wait runtime final cleanup/proof and parent final rebind signal. No corrected snapshot or deployment yet.

Corrected candidate ready for parent review: `evidence/auth-retention-corrected-source-snapshot`,87files, manifestSHA2bd6c0aa4cb114fecbf9064eebaf10979f5df23381a5ad659597c283f7d3925d. Explicit10-file whitelist replaces runner/credential_state and selected tests/operator drivers only; OLD frozen API/CLI/static + matching tests preserved. Candidate suite541passed/0skips, exact-secret scan and actual Omarchy preflight passed. Fresh stage `/tmp/cwb-auth-corrected-20260917-r2/snapshot`, root-owned/shared-group read/traverse only. Binding `evidence/auth-corrected-candidate-binding.json`, final reviewed live completion proof binds runner76e3aa39. HOLD execute until runtime actual CLI401 proof and parent review. No image build/source publication/state initialization/restart performed.

## Auth rollout current checkpoint

2026-09-17: Auth/retention/image rollout deployed on Omarchy successfully, frozen manifest 1b1786df0b8be52ad14604b7085a15b18e3ce111a51c20dd8ef660f4cbbcb8e0. Exact receipt evidence/auth-corrected-deployment-receipt.json, binding evidence/auth-resumed-deployment-binding.json. Image f1b15bb81917de9b48df7c9b5c2be438e686f692b42bdaa58eb2aeedf5b9ea0b; old408eb pin retained. Failed build and private-dir initialization receipts preserved. Setgid inherited2700 is private and now accepted; no chmod/reset/quarantine clearing. Both v2 services authenticated ready; legacy PID974255 unchanged. Provider three-attempt qualifier session72756 currently running; Docker observer49294 (600sec max) writing evidence/auth-live-image-observations.jsonl. Remote stage /tmp/cwb-auth-corrected-20260917-r2/snapshot. P15 API/CLI/static worktree changes are excluded.

Auth checkpoint completed: all three actual Claude attempts passed independent verification; qualifier exited0, observer stopped. New session95eea001-1c8f-427b-97f2-061b95546818 attempts5aa0a4da-072f-4d84-871f-23cd9ed22c92 and8857565d-a8ba-4c11-82bf-0a4cb2c20a22 used newf1b15 image; historicalsession followup1f9930de-1248-474d-ac1c-754b8525a138 used old408eb. Six actual Docker agent/verifier image observations bound; downloaded patch apply/hash/mode checks and liveSSE passed, reversible retention controls restored originalkeep. All exact staged capsulefiles absent, dirs2700/file0600 ownedworker, zero ownedcontainers/networks/pendingattempts, legacyPID unchanged. Evidence/auth-live-qualification-binding.json binds receipts. Next bounded parent task: prepare P15 API-only source update (api/cli/result_bundle/dashboard/static, matchingtests); no image/worker/runner/resource-monitor/preview changes and no live mutation until parent reviews concrete plan.

P15 API-only rollout complete: exact frozenmanifest b61702a9fee60b0b8802ea2574efdf8d3850877ad005870ca13d91ca3b8d8ee1 published five files. API PID1240083, worker1222709/legacy974255 unchanged. HTTPS raw+CLI resultZIP passed (21054bytes,10entries,6artifacts); deterministic/hash/no-clobber and two TESTprincipal denials passed, principals revoked finally, priorgrants unchanged. No providercalls. Receipt binding evidence/p15-api-live-binding.json. One wrapper setupfailure (missing shared_group=True) preserved; corrected without app changes. Parent owns browser proof and next local dashboard input-upload work. Do not refresh frozen snapshot from evolving worktree.

Hermes direction pivot: the original operator clarified Hermes must always be the agent runtime, with configurable Claude/Codex/Grok backends and pstack role routing. Parent owns spec reconciliation and existing community pstack-hermes candidate. Legacy stager paused (legacy_import.py/tests40pass; unreviewed, no live reads/import). Read-only Hermes audit complete: docs/HERMES-RUNTIME-CONTRACT.md and evidence/hermes-runtime-source-audit.json / hermes-omarchy-install-audit.json. Both checkout commit3b0e392e5a6922034feccac5771041ac78467757 version0.21.3,10 common sourcehashes match. Native JSONL available; no per-child model fields in delegate_task, shared delegation config applies to every child. Mixed-model roles need separately configured supervised Hermes processes or trusted extension; no global config rewrites. Builtin claude-code aliases anthropic, not CLIbridge. Legacyclaude-cli bridge plugin unqualified/currenthardenedimage removes binary. NativeCodex/XAI auth paths exist but actual dedicatedaccount availability uninspected. No provider calls/credentials/configvalue reads or remote mutations. Synthetic standalone emitter proof passed, explicitly not CLI/provider startup proof. Existing directClaude jobs retained unchanged. Parent receives minimal contract and capability gaps.
