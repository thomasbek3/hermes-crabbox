# Provider qualification source closure

The current executor and supervisor import `provider_protocol.py`, which imports `native_responses.py`. All three explicit controller source lists now include both modules: `qualify-native-supervised-linux.py`, `qualify-supervised-provider-linux.py`, and `qualify-real-provider-guarded.py`. The supervised-only harness also now includes `provider_recovery.py`, an existing wrapper dependency. The real qualification example has the exact new set of null source hashes; it remains unauthorized and non-executable until its independent prerequisites are satisfied.

The provider container needs `native_responses.py` alongside the previous six modules. The builder's exact manifest whitelist now includes it. The Dockerfile already copies the package and imports `provider_main`, so its commands did not change. `scripts/prepare-provider-image-context.py NEW_DIRECTORY` prepares only those seven current source files, the existing Dockerfile, and their SHA256 manifest. It refuses an existing destination; it does not run Docker, SSH, install dependencies, read credentials, or replace previous contexts. The candidate builder now requires `--context NEW_DIRECTORY --receipt NEW_JSON --build-log NEW_LOG`. It validates the complete context before SSH, refuses existing evidence files, builds the validated in-memory archive with networking disabled, and records failures without replacing historical receipts. Its remote host and base image checks remain pinned. No build was executed for this change.

Candidate receipts explicitly require review and runtime qualification of current source; they do not inherit approval from an older source review. Neither the seven-file context nor successful local imports qualify a new image, provider account, runtime path, or inference call. Historical evidence, image receipts, source snapshots, and the existing staging directory were left untouched by this maintenance.

Local verification: six tests copy each exact allowlist into a temporary isolated package and import it in a fresh interpreter, rejecting socket connections/subprocess launches and asserting every imported Workbench module came from that copy. The missing-native-module negative control fails as expected instead of finding the complete checkout. Tests also verify the context's file set/hashes, builder whitelist parity, existing-destination refusal, and real qualification example parity. This is import/package closure for the tested entrypoints, not execution of every lazy branch. Eighty-six existing qualification tests pass. Receipts: `evidence/provider-source-closure-tests.xml`, `evidence/provider-source-closure-regressions.xml`, and `evidence/provider-source-closure-binding.json`.

Builder regression checkpoint: fourteen builder/source-closure tests pass, including explicit context selection, stale-source refusal, existing-evidence refusal before remote calls, exact archive contents, failure receipts, bounded remote inspections, and timeout evidence preserving the candidate tag, exact source/archive binding, partial logs, and an explicitly unknown remote build outcome. Remote operations were mocked; this is not an image build. Receipt: `evidence/provider-candidate-builder-tests.xml`.
